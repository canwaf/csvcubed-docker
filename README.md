# csvcubed-docker
This repository is a proof of concept which allows users to check in csv files along with a qube-config.json file to automaticlaly generate csv-w.

It uses github actions to watch the contents of the direction; upon commit it uses databaker-docker to build the csv-w.